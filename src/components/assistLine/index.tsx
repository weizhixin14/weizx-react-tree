import React from 'react';
import type { AssistLine as AssistLineProps } from '~types/global';

const AssistLine: React.FC<AssistLineProps> = ({
    assistLineType
}) => {
    return (
        <div></div>
    );
};

export default AssistLine;
