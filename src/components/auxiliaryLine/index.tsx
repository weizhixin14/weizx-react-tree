import React from 'react';
import type { AuxiliaryLineProps } from '~types/global';

const AuxiliaryLine: React.FC<AuxiliaryLineProps> = ({
    auxiliaryType
}) => {
    return (
        <div></div>
    );
};

export default AuxiliaryLine;
